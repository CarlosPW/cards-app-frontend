import type { NextPage } from "next";
import styled from "styled-components";
import { Button } from "../components/ui";

const Home: NextPage = () => {
  return (
    <div>
      <Button>Boton</Button>
    </div>
  );
};

export default Home;
